eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/, http://localhost:8762/eureka/
    #自定义链接
    instance:
      hostname: clientName
spring:
  application:
    name: client