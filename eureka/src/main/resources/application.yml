eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/
    register-with-eureka: false
  #自我保护模式（开发环境使用，方便调用微服务的时候不会出错，生产环境不要这个配置）
  server:
    enable-self-preservation: false
spring:
  application:
    name: eureka
server:
  port: 8761
